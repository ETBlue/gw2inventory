/**
 * Utility functions for chunking arrays
 */

/**
 * Helper function to split an array into chunks of specified size
 * Provides a consistent API across the codebase using lodash's chunk function
 */
export { chunk as chunkArray } from "lodash"
