import type { Outfit } from "@gw2api/types/data/outfit"

// Account outfit IDs - array of numeric IDs
export type AccountOutfits = number[]

// Re-export Outfit type from @gw2api/types for convenience
export type { Outfit }
